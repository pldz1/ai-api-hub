import { videoParamList, videoProviderKeys } from "@/ai-capability/video";

export const videoParamPresetList = videoParamList;

export { videoProviderKeys as videoModelProviderList, videoProviderKeys, videoModelBindings, videoCatalogModelIds } from "@/ai-capability/video";
